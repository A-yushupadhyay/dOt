<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/views/assets/logo-01.png">

    <title>DocOnTime - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- Include Socket.io -->
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <!-- Add any other global CSS/JS links here -->
  <style>
    body {
        margin: 0;
        padding: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: #f0f4f8;
    }

    .main-container {
        text-align: center;
        padding: 60px 20px;
    }

    h1 {
        color: #2c3e50;
        font-size: 3rem;
        margin-bottom: 20px;
    }

    p {
        font-size: 1.2rem;
        color: #34495e;
    }

    /* Modal Styling */
    .custom-modal-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: rgba(0, 0, 0, 0.6);
        z-index: 9999;
        justify-content: center;
        align-items: center;
    }

    .custom-modal-overlay.fade-out {
        animation: fadeOut 0.4s forwards;
    }

    .custom-modal {
        background-color: #ffffff;
        padding: 2rem 2.5rem;
        border-radius: 2rem;
        text-align: center;
        max-width: 450px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        animation: popIn 0.5s ease-in-out;
    }

    .custom-modal h2 {
        margin-top: 0;
        color: #2ecc71;
        font-size: 2rem;
    }

    .custom-modal p {
        color: #555;
        font-size: 1.1rem;
        margin: 10px 0;
    }

    .close-button {
        background-color: #e74c3c;
        color: white;
        border: none;
        padding: 10px 18px;
        border-radius: 1rem;
        cursor: pointer;
        margin-top: 15px;
        font-size: 1rem;
        transition: background 0.3s ease;
    }

    .close-button:hover {
        background-color: #c0392b;
    }

    @keyframes popIn {
        0% {
            transform: scale(0.6);
            opacity: 0;
        }
        100% {
            transform: scale(1);
            opacity: 1;
        }
    }

    @keyframes fadeOut {
        to {
            opacity: 0;
            visibility: hidden;
        }
    }
</style>
</head>
<body class="bg-gray-100 dark:bg-gray-900 dark:text-white">
  <%- body %> <!-- This will inject content from extended views -->
</body>
</html>
